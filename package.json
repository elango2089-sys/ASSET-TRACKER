{
  "name": "synergy-asset-tracker",
  "version": "1.0.0",
  "description": "Asset tracker for Synergy Global Sourcing with shared data backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "if (!(Test-Path dist)) { mkdir dist }; cp index.html, styles.css, app.js, suppliers.js, logo.png, manifest.json, sw.js dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  }
}
